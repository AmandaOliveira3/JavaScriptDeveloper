

let listaNomes = ['Aline', 'Vanessa', 'Vitor', 'Caio', 'Felipe'];


for (let i = 0; i < listaNomes.length; i++) {
    if (listaNomes[i].charAt(0) === 'V') {
        console.log(listaNomes[i]);
    }
}